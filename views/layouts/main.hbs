<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ title }}</title>
    </head>
    <body>
        {{#if error }}
        <div class="error">{{error.message}}</div>
        {{/if }}

        {{{ body }}}
    </body>
</html>